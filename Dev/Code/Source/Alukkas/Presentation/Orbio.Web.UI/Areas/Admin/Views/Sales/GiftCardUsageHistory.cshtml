@model List<Orbio.Web.UI.Areas.Admin.Models.Sales.GiftCardUsageHistoryModel>

<div class="col-md-12">
    @*<div class="refresh_xl">
            <span class="refresh_button" style="height:32px"><label></label></span>
            <span style="margin-left:5px"><a href="@Url.Action("AddOrEditSpecificationAttributeOption", "Attribute", new { id = 0, Spec = Model.Id })" data-toggle="modal"><input type="button" class="man_search" value="Add a new value"></a></span>
        </div>*@
    <div class="col-md-9 main_order_table" style="width:100%">
        <table class=" item_admin_order">
            <thead>
            <td>Used Amount</td>
            <td>Order</td>
            <td>Used Date</td>
            </thead>
            <tbody>
                @if(Model.Count>0)
                {
                    int count=1;
                    foreach(var item in this.Model)
                    {
                        if (count == 1)
                        {
                            @:<tr class="first_row">
                          count++;
                         }
                         else{
                            @:<tr class="second_row">
                             count = 1;
                         }
                        <td class="varient_align" style="text-align:center">@item.UsedAmount</td>
                        <td class="varient_align" style="text-align:center"><a href="">@item.OrderId</a></td>
                        <td class="varient_align" style="text-align:center">@item.UsedOn</td>
                        @:</tr>
                        }
                }
                else
                {
                    <tr>
                        <td colspan="3">
                            No history to display
                        </td>
                    </tr>
                }
                </tbody>
        </table>
        </div>
    </div>
                  
                    


                      
                           